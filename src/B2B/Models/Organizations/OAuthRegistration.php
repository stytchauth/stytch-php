<?php

// !!!
// WARNING: This file is autogenerated
// Only modify code within MANUAL() sections
// or your changes may be overwritten later!
// !!!

namespace Stytch\B2B\Models\Organizations;

class OAuthRegistration
{
    /**
    * Denotes the OAuth identity provider that the user has authenticated with, e.g. Google, Microsoft, GitHub
    * etc.
     */
    public string $providerType;
    /**
    * The unique identifier for the User within a given OAuth provider. Also commonly called the `sub` or
    * "Subject field" in OAuth protocols.
     */
    public string $providerSubject;
    /** The unique ID of an OAuth registration. */
    public string $memberOAuthRegistrationId;
    /**
    * If available, the `profile_picture_url` is a URL of the User's profile picture set in OAuth identity the
    * provider that the User has authenticated with, e.g. Google profile picture.
     */
    public ?string $profilePictureURL = null;
    /**
    * If available, the `locale` is the Member's locale set in the OAuth identity provider that the user has
    * authenticated with.
     */
    public ?string $locale = null;

    public function __construct(
        string $providerType,
        string $providerSubject,
        string $memberOAuthRegistrationId,
        ?string $profilePictureURL = null,
        ?string $locale = null
    ) {
        $this->providerType = $providerType;
        $this->providerSubject = $providerSubject;
        $this->memberOAuthRegistrationId = $memberOAuthRegistrationId;
        $this->profilePictureURL = $profilePictureURL;
        $this->locale = $locale;
    }

    /**
     * Create an instance from an array
     *
     * @param array $data
     * @return static
     */
    public static function fromArray(array $data): static
    {
        return new static(
            $data['provider_type'],
            $data['provider_subject'],
            $data['member_oauth_registration_id'],
            $data['profile_picture_url'] ?? null,
            $data['locale'] ?? null
        );
    }

    /**
     * Convert to array
     *
     * @return array
     */
    public function toArray(): array
    {
        return [
            'provider_type' => $this->providerType,
            'provider_subject' => $this->providerSubject,
            'member_oauth_registration_id' => $this->memberOAuthRegistrationId,
            'profile_picture_url' => $this->profilePictureURL,
            'locale' => $this->locale,
        ];
    }
}
