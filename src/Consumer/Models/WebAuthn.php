<?php

// !!!
// WARNING: This file is autogenerated
// Only modify code within MANUAL() sections
// or your changes may be overwritten later!
// !!!

namespace Stytch\Consumer\Models;

class WebAuthnCredential
{
    public string $credential_id;
    public string $webauthn_registration_id;
    public string $type;
    public string $public_key;

    public function __construct(
        string $credential_id,
        string $webauthn_registration_id,
        string $type,
        string $public_key
    ) {
        $this->credential_id = $credential_id;
        $this->webauthn_registration_id = $webauthn_registration_id;
        $this->type = $type;
        $this->public_key = $public_key;
    }

    /**
     * Create an instance from an array
     *
     * @param array $data
     * @return static
     */
    public static function fromArray(array $data): static
    {
        return new static(
            $data['credential_id'],
            $data['webauthn_registration_id'],
            $data['type'],
            $data['public_key']
        );
    }

    /**
     * Convert to array
     *
     * @return array
     */
    public function toArray(): array
    {
        return [
            'credential_id' => $this->credential_id,
            'webauthn_registration_id' => $this->webauthn_registration_id,
            'type' => $this->type,
            'public_key' => $this->public_key,
        ];
    }
}

class AuthenticateResponse
{
    public string $user_id;
    public string $webauthn_registration_id;
    public string $session_token;
    public string $session_jwt;
    public User $user;
    public ?Session $session;
    public ?DeviceInfo $user_device;

    public function __construct(
        string $user_id,
        string $webauthn_registration_id,
        string $session_token,
        string $session_jwt,
        User $user,
        ?Session $session,
        ?DeviceInfo $user_device
    ) {
        $this->user_id = $user_id;
        $this->webauthn_registration_id = $webauthn_registration_id;
        $this->session_token = $session_token;
        $this->session_jwt = $session_jwt;
        $this->user = $user;
        $this->session = $session;
        $this->user_device = $user_device;
    }

    /**
     * Create an instance from an array
     *
     * @param array $data
     * @return static
     */
    public static function fromArray(array $data): static
    {
        return new static(
            $data['user_id'],
            $data['webauthn_registration_id'],
            $data['session_token'],
            $data['session_jwt'],
            $data['user'],
            $data['session'] ?? null,
            $data['user_device'] ?? null
        );
    }

    /**
     * Convert to array
     *
     * @return array
     */
    public function toArray(): array
    {
        return [
            'user_id' => $this->user_id,
            'webauthn_registration_id' => $this->webauthn_registration_id,
            'session_token' => $this->session_token,
            'session_jwt' => $this->session_jwt,
            'user' => $this->user,
            'session' => $this->session,
            'user_device' => $this->user_device,
        ];
    }
}

class AuthenticateStartResponse
{
    public string $user_id;
    public string $public_key_credential_request_options;

    public function __construct(
        string $user_id,
        string $public_key_credential_request_options
    ) {
        $this->user_id = $user_id;
        $this->public_key_credential_request_options = $public_key_credential_request_options;
    }

    /**
     * Create an instance from an array
     *
     * @param array $data
     * @return static
     */
    public static function fromArray(array $data): static
    {
        return new static(
            $data['user_id'],
            $data['public_key_credential_request_options']
        );
    }

    /**
     * Convert to array
     *
     * @return array
     */
    public function toArray(): array
    {
        return [
            'user_id' => $this->user_id,
            'public_key_credential_request_options' => $this->public_key_credential_request_options,
        ];
    }
}

class ListCredentialsResponse
{
    public array $credentials;

    public function __construct(
        array $credentials
    ) {
        $this->credentials = $credentials;
    }

    /**
     * Create an instance from an array
     *
     * @param array $data
     * @return static
     */
    public static function fromArray(array $data): static
    {
        return new static(
            $data['credentials']
        );
    }

    /**
     * Convert to array
     *
     * @return array
     */
    public function toArray(): array
    {
        return [
            'credentials' => $this->credentials,
        ];
    }
}

class RegisterResponse
{
    public string $user_id;
    public string $webauthn_registration_id;
    public string $session_token;
    public string $session_jwt;
    public User $user;
    public ?Session $session;
    public ?DeviceInfo $user_device;

    public function __construct(
        string $user_id,
        string $webauthn_registration_id,
        string $session_token,
        string $session_jwt,
        User $user,
        ?Session $session,
        ?DeviceInfo $user_device
    ) {
        $this->user_id = $user_id;
        $this->webauthn_registration_id = $webauthn_registration_id;
        $this->session_token = $session_token;
        $this->session_jwt = $session_jwt;
        $this->user = $user;
        $this->session = $session;
        $this->user_device = $user_device;
    }

    /**
     * Create an instance from an array
     *
     * @param array $data
     * @return static
     */
    public static function fromArray(array $data): static
    {
        return new static(
            $data['user_id'],
            $data['webauthn_registration_id'],
            $data['session_token'],
            $data['session_jwt'],
            $data['user'],
            $data['session'] ?? null,
            $data['user_device'] ?? null
        );
    }

    /**
     * Convert to array
     *
     * @return array
     */
    public function toArray(): array
    {
        return [
            'user_id' => $this->user_id,
            'webauthn_registration_id' => $this->webauthn_registration_id,
            'session_token' => $this->session_token,
            'session_jwt' => $this->session_jwt,
            'user' => $this->user,
            'session' => $this->session,
            'user_device' => $this->user_device,
        ];
    }
}

class RegisterStartResponse
{
    public string $user_id;
    public string $public_key_credential_creation_options;

    public function __construct(
        string $user_id,
        string $public_key_credential_creation_options
    ) {
        $this->user_id = $user_id;
        $this->public_key_credential_creation_options = $public_key_credential_creation_options;
    }

    /**
     * Create an instance from an array
     *
     * @param array $data
     * @return static
     */
    public static function fromArray(array $data): static
    {
        return new static(
            $data['user_id'],
            $data['public_key_credential_creation_options']
        );
    }

    /**
     * Convert to array
     *
     * @return array
     */
    public function toArray(): array
    {
        return [
            'user_id' => $this->user_id,
            'public_key_credential_creation_options' => $this->public_key_credential_creation_options,
        ];
    }
}

class UpdateResponse
{
    public ?WebAuthnRegistration $webauthn_registration;

    public function __construct(
        ?WebAuthnRegistration $webauthn_registration
    ) {
        $this->webauthn_registration = $webauthn_registration;
    }

    /**
     * Create an instance from an array
     *
     * @param array $data
     * @return static
     */
    public static function fromArray(array $data): static
    {
        return new static(
            $data['webauthn_registration'] ?? null
        );
    }

    /**
     * Convert to array
     *
     * @return array
     */
    public function toArray(): array
    {
        return [
            'webauthn_registration' => $this->webauthn_registration,
        ];
    }
}

