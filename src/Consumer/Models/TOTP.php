<?php

// !!!
// WARNING: This file is autogenerated
// Only modify code within MANUAL() sections
// or your changes may be overwritten later!
// !!!

namespace Stytch\Consumer\Models;

class TOTPsGetRecoveryCodesResponse
{
    public string $user_id;
    public ?array $totps;

    public function __construct(
        string $user_id,
        ?array $totps
    ) {
        $this->user_id = $user_id;
        $this->totps = $totps;
    }

    /**
     * Create an instance from an array
     *
     * @param array $data
     * @return static
     */
    public static function fromArray(array $data): static
    {
        return new static(
            $data['user_id'],
            $data['totps'] ?? null
        );
    }

    /**
     * Convert to array
     *
     * @return array
     */
    public function toArray(): array
    {
        return [
            'user_id' => $this->user_id,
            'totps' => $this->totps,
        ];
    }
}

class TOTPsRecoverResponse
{
    public string $totp_id;
    public string $user_id;
    public string $session_token;
    public ?Session $session;
    public string $session_jwt;
    public User $user;
    public ?DeviceInfo $user_device;

    public function __construct(
        string $totp_id,
        string $user_id,
        string $session_token,
        ?Session $session,
        string $session_jwt,
        User $user,
        ?DeviceInfo $user_device
    ) {
        $this->totp_id = $totp_id;
        $this->user_id = $user_id;
        $this->session_token = $session_token;
        $this->session = $session;
        $this->session_jwt = $session_jwt;
        $this->user = $user;
        $this->user_device = $user_device;
    }

    /**
     * Create an instance from an array
     *
     * @param array $data
     * @return static
     */
    public static function fromArray(array $data): static
    {
        return new static(
            $data['totp_id'],
            $data['user_id'],
            $data['session_token'],
            $data['session'] ?? null,
            $data['session_jwt'],
            $data['user'],
            $data['user_device'] ?? null
        );
    }

    /**
     * Convert to array
     *
     * @return array
     */
    public function toArray(): array
    {
        return [
            'totp_id' => $this->totp_id,
            'user_id' => $this->user_id,
            'session_token' => $this->session_token,
            'session' => $this->session,
            'session_jwt' => $this->session_jwt,
            'user' => $this->user,
            'user_device' => $this->user_device,
        ];
    }
}

class TOTPsAuthenticateResponse
{
    public string $user_id;
    public string $session_token;
    public ?Session $session;
    public string $totp_id;
    public string $session_jwt;
    public User $user;
    public ?DeviceInfo $user_device;

    public function __construct(
        string $user_id,
        string $session_token,
        ?Session $session,
        string $totp_id,
        string $session_jwt,
        User $user,
        ?DeviceInfo $user_device
    ) {
        $this->user_id = $user_id;
        $this->session_token = $session_token;
        $this->session = $session;
        $this->totp_id = $totp_id;
        $this->session_jwt = $session_jwt;
        $this->user = $user;
        $this->user_device = $user_device;
    }

    /**
     * Create an instance from an array
     *
     * @param array $data
     * @return static
     */
    public static function fromArray(array $data): static
    {
        return new static(
            $data['user_id'],
            $data['session_token'],
            $data['session'] ?? null,
            $data['totp_id'],
            $data['session_jwt'],
            $data['user'],
            $data['user_device'] ?? null
        );
    }

    /**
     * Convert to array
     *
     * @return array
     */
    public function toArray(): array
    {
        return [
            'user_id' => $this->user_id,
            'session_token' => $this->session_token,
            'session' => $this->session,
            'totp_id' => $this->totp_id,
            'session_jwt' => $this->session_jwt,
            'user' => $this->user,
            'user_device' => $this->user_device,
        ];
    }
}

class TOTP
{
    public string $totp_id;
    public bool $verified;
    public array $recovery_codes;

    public function __construct(
        string $totp_id,
        bool $verified,
        array $recovery_codes
    ) {
        $this->totp_id = $totp_id;
        $this->verified = $verified;
        $this->recovery_codes = $recovery_codes;
    }

    /**
     * Create an instance from an array
     *
     * @param array $data
     * @return static
     */
    public static function fromArray(array $data): static
    {
        return new static(
            $data['totp_id'],
            $data['verified'],
            $data['recovery_codes']
        );
    }

    /**
     * Convert to array
     *
     * @return array
     */
    public function toArray(): array
    {
        return [
            'totp_id' => $this->totp_id,
            'verified' => $this->verified,
            'recovery_codes' => $this->recovery_codes,
        ];
    }
}

class TOTPsCreateResponse
{
    public string $totp_id;
    public string $secret;
    public string $qr_code;
    public array $recovery_codes;
    public User $user;
    public string $user_id;

    public function __construct(
        string $totp_id,
        string $secret,
        string $qr_code,
        array $recovery_codes,
        User $user,
        string $user_id
    ) {
        $this->totp_id = $totp_id;
        $this->secret = $secret;
        $this->qr_code = $qr_code;
        $this->recovery_codes = $recovery_codes;
        $this->user = $user;
        $this->user_id = $user_id;
    }

    /**
     * Create an instance from an array
     *
     * @param array $data
     * @return static
     */
    public static function fromArray(array $data): static
    {
        return new static(
            $data['totp_id'],
            $data['secret'],
            $data['qr_code'],
            $data['recovery_codes'],
            $data['user'],
            $data['user_id']
        );
    }

    /**
     * Convert to array
     *
     * @return array
     */
    public function toArray(): array
    {
        return [
            'totp_id' => $this->totp_id,
            'secret' => $this->secret,
            'qr_code' => $this->qr_code,
            'recovery_codes' => $this->recovery_codes,
            'user' => $this->user,
            'user_id' => $this->user_id,
        ];
    }
}

