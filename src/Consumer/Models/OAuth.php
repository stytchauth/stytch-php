<?php

// !!!
// WARNING: This file is autogenerated
// Only modify code within MANUAL() sections
// or your changes may be overwritten later!
// !!!

namespace Stytch\Consumer\Models;

class ProviderValues
{
    public string $access_token;
    public string $refresh_token;
    public string $id_token;
    public array $scopes;
    public ?string $expires_at;

    public function __construct(
        string $access_token,
        string $refresh_token,
        string $id_token,
        array $scopes,
        ?string $expires_at
    ) {
        $this->access_token = $access_token;
        $this->refresh_token = $refresh_token;
        $this->id_token = $id_token;
        $this->scopes = $scopes;
        $this->expires_at = $expires_at;
    }

    /**
     * Create an instance from an array
     *
     * @param array $data
     * @return static
     */
    public static function fromArray(array $data): static
    {
        return new static(
            $data['access_token'],
            $data['refresh_token'],
            $data['id_token'],
            $data['scopes'],
            $data['expires_at'] ?? null
        );
    }

    /**
     * Convert to array
     *
     * @return array
     */
    public function toArray(): array
    {
        return [
            'access_token' => $this->access_token,
            'refresh_token' => $this->refresh_token,
            'id_token' => $this->id_token,
            'scopes' => $this->scopes,
            'expires_at' => $this->expires_at,
        ];
    }
}

class AttachResponse
{
    public string $oauth_attach_token;

    public function __construct(
        string $oauth_attach_token
    ) {
        $this->oauth_attach_token = $oauth_attach_token;
    }

    /**
     * Create an instance from an array
     *
     * @param array $data
     * @return static
     */
    public static function fromArray(array $data): static
    {
        return new static(
            $data['oauth_attach_token']
        );
    }

    /**
     * Convert to array
     *
     * @return array
     */
    public function toArray(): array
    {
        return [
            'oauth_attach_token' => $this->oauth_attach_token,
        ];
    }
}

class AuthenticateResponse
{
    public string $user_id;
    public string $provider_subject;
    public string $provider_type;
    public string $session_token;
    public string $session_jwt;
    public ProviderValues $provider_values;
    public User $user;
    public bool $reset_sessions;
    public string $oauth_user_registration_id;
    public ?Session $user_session;
    public ?DeviceInfo $user_device;

    public function __construct(
        string $user_id,
        string $provider_subject,
        string $provider_type,
        string $session_token,
        string $session_jwt,
        ProviderValues $provider_values,
        User $user,
        bool $reset_sessions,
        string $oauth_user_registration_id,
        ?Session $user_session,
        ?DeviceInfo $user_device
    ) {
        $this->user_id = $user_id;
        $this->provider_subject = $provider_subject;
        $this->provider_type = $provider_type;
        $this->session_token = $session_token;
        $this->session_jwt = $session_jwt;
        $this->provider_values = $provider_values;
        $this->user = $user;
        $this->reset_sessions = $reset_sessions;
        $this->oauth_user_registration_id = $oauth_user_registration_id;
        $this->user_session = $user_session;
        $this->user_device = $user_device;
    }

    /**
     * Create an instance from an array
     *
     * @param array $data
     * @return static
     */
    public static function fromArray(array $data): static
    {
        return new static(
            $data['user_id'],
            $data['provider_subject'],
            $data['provider_type'],
            $data['session_token'],
            $data['session_jwt'],
            $data['provider_values'],
            $data['user'],
            $data['reset_sessions'],
            $data['oauth_user_registration_id'],
            $data['user_session'] ?? null,
            $data['user_device'] ?? null
        );
    }

    /**
     * Convert to array
     *
     * @return array
     */
    public function toArray(): array
    {
        return [
            'user_id' => $this->user_id,
            'provider_subject' => $this->provider_subject,
            'provider_type' => $this->provider_type,
            'session_token' => $this->session_token,
            'session_jwt' => $this->session_jwt,
            'provider_values' => $this->provider_values,
            'user' => $this->user,
            'reset_sessions' => $this->reset_sessions,
            'oauth_user_registration_id' => $this->oauth_user_registration_id,
            'user_session' => $this->user_session,
            'user_device' => $this->user_device,
        ];
    }
}

